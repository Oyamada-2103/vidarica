<div class ="row">
  <h5>scene：</h5>
  <% @tag_list.each do |list| %>
    <% if list.genre.id == 2 %>
      <%= link_to list.tag_name, tag_post_images_path(tag_id: list.id) %>
      <%= "(#{list.post_tags.count})"%>
      <%= "　"%>
    <% end %>
  <% end %>
  <%= link_to "全てのタグを見る", root_path %>
</div>


<div class = "row">
  <h1>投稿一覧</h1>
</div>
<br>
<!--インデックス部分をテンプレート化していれる-->
<%= render partial: "post_images/index", locals: { post_images: @post_images } %>

