<!--いいね機能-->
<% if post_image.favorited_by?(current_user) %>
  <p>
    <%= link_to post_image_favorite_path(post_image), method: :delete do %>
      ♥<%= post_image.favorites.count %>Like
    <% end %>
  </p>
<% else %>
  <p>
    <%= link_to post_image_favorites_path(post_image), method: :post do %>
      ♡<%= post_image.favorites.count %>Like
    <% end %>
  </p>
<% end %>