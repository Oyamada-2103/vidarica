<div class="container">
  <div class="row">
    <p><%= @user.user_name %>さんがフォローしているユーザー一覧</p>
  </div>

  <div class="row">
  <% @users.each do |user| %>
    <%= render partial: "users/user_info", locals: { user: user } %>
  <% end %>
  </div>

</div>